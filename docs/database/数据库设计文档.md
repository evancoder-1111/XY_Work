# 数据库设计文档

## 数据库信息

- **数据库类型**: MySQL 8.0+
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci
- **数据库名**: `xy_portal`

## 表结构设计

### 1. 用户表 (users)

存储用户基本信息。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键，自增 |
| username | VARCHAR | 255 | NOT NULL | - | 用户名，唯一 |
| password | VARCHAR | 255 | NOT NULL | - | 密码（加密存储） |
| nickname | VARCHAR | 255 | NULL | - | 昵称 |
| email | VARCHAR | 255 | NULL | - | 邮箱 |
| avatar | VARCHAR | 512 | NULL | - | 头像URL |
| role | VARCHAR | 50 | NOT NULL | 'USER' | 角色：ADMIN, USER |
| status | VARCHAR | 20 | NOT NULL | 'ACTIVE' | 状态：ACTIVE, INACTIVE |
| created_at | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY (`id`)
- UNIQUE KEY `uk_username` (`username`)
- KEY `idx_email` (`email`)
- KEY `idx_role` (`role`)

**SQL 创建语句**:
```sql
CREATE TABLE `users` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `username` VARCHAR(255) NOT NULL,
  `password` VARCHAR(255) NOT NULL,
  `nickname` VARCHAR(255) DEFAULT NULL,
  `email` VARCHAR(255) DEFAULT NULL,
  `avatar` VARCHAR(512) DEFAULT NULL,
  `role` VARCHAR(50) NOT NULL DEFAULT 'USER',
  `status` VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_email` (`email`),
  KEY `idx_role` (`role`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';
```

### 2. 门户入口表 (portal_entries)

存储系统入口配置信息。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键，自增 |
| name | VARCHAR | 255 | NOT NULL | - | 系统名称 |
| description | VARCHAR | 512 | NULL | - | 系统描述 |
| icon | VARCHAR | 255 | NULL | - | 图标（Emoji 或 URL） |
| url | VARCHAR | 512 | NOT NULL | - | 访问地址 |
| category | VARCHAR | 50 | NULL | - | 分类：业务系统、协作工具、管理系统 |
| status | VARCHAR | 20 | NOT NULL | 'ACTIVE' | 状态：ACTIVE, INACTIVE |
| sort_order | INT | - | NULL | 0 | 排序顺序 |
| created_by | BIGINT | - | NULL | - | 创建人ID（关联users.id） |
| created_at | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY (`id`)
- KEY `idx_category` (`category`)
- KEY `idx_status` (`status`)
- KEY `idx_sort_order` (`sort_order`)
- KEY `idx_created_by` (`created_by`)

**SQL 创建语句**:
```sql
CREATE TABLE `portal_entries` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(255) NOT NULL,
  `description` VARCHAR(512) DEFAULT NULL,
  `icon` VARCHAR(255) DEFAULT NULL,
  `url` VARCHAR(512) NOT NULL,
  `category` VARCHAR(50) DEFAULT NULL,
  `status` VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
  `sort_order` INT DEFAULT 0,
  `created_by` BIGINT DEFAULT NULL,
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_category` (`category`),
  KEY `idx_status` (`status`),
  KEY `idx_sort_order` (`sort_order`),
  KEY `idx_created_by` (`created_by`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='门户入口表';
```

### 3. 用户角色表 (user_roles)

存储用户角色关联信息（扩展表，用于未来权限扩展）。

| 字段名 | 类型 | 长度 | 是否为空 | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NOT NULL | AUTO_INCREMENT | 主键，自增 |
| user_id | BIGINT | - | NOT NULL | - | 用户ID（关联users.id） |
| role_code | VARCHAR | 50 | NOT NULL | - | 角色代码：ADMIN, MANAGER, PRODUCT_MANAGER, DEVELOPER, TESTER |
| created_at | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- PRIMARY KEY (`id`)
- UNIQUE KEY `uk_user_role` (`user_id`, `role_code`)
- KEY `idx_user_id` (`user_id`)
- KEY `idx_role_code` (`role_code`)

**SQL 创建语句**:
```sql
CREATE TABLE `user_roles` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `role_code` VARCHAR(50) NOT NULL,
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_role` (`user_id`, `role_code`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_role_code` (`role_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户角色表';
```

## ER 图

```
┌─────────────┐         ┌──────────────┐
│    users    │         │ portal_entries│
├─────────────┤         ├──────────────┤
│ id (PK)     │         │ id (PK)       │
│ username    │         │ name          │
│ password    │         │ description   │
│ nickname    │         │ icon          │
│ email       │         │ url           │
│ avatar      │         │ category      │
│ role        │         │ status        │
│ status      │         │ sort_order    │
│ created_at  │         │ created_by    │
│ updated_at  │         │ created_at    │
└─────────────┘         │ updated_at    │
       │                └──────────────┘
       │                        │
       │                        │
       │                        │
       │                ┌───────┘
       │                │
       │                │ (created_by)
       │                │
       ▼                ▼
┌─────────────┐
│ user_roles │
├─────────────┤
│ id (PK)     │
│ user_id (FK)│──────┐
│ role_code   │      │
│ created_at  │      │
└─────────────┘      │
                     │ (user_id)
                     │
                     │
                     ▼
              ┌─────────────┐
              │    users     │
              └─────────────┘
```

## 初始化数据

### 用户数据

```sql
-- 插入管理员用户（密码：admin123，实际应使用 BCrypt 加密）
INSERT INTO `users` (`username`, `password`, `nickname`, `email`, `role`, `status`) VALUES
('admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iwy8pK0O', '管理员', 'admin@example.com', 'ADMIN', 'ACTIVE'),
('user1', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iwy8pK0O', '用户1', 'user1@example.com', 'USER', 'ACTIVE');
```

### 门户入口数据

```sql
-- 插入示例入口
INSERT INTO `portal_entries` (`name`, `description`, `icon`, `url`, `category`, `status`, `sort_order`) VALUES
('ERP系统', '企业资源规划系统', '📊', 'https://erp.example.com', '业务系统', 'ACTIVE', 1),
('CRM系统', '客户关系管理系统', '👥', 'https://crm.example.com', '业务系统', 'ACTIVE', 2),
('OA系统', '办公自动化系统', '📝', 'https://oa.example.com', '协作工具', 'ACTIVE', 3),
('HR系统', '人力资源管理系统', '👔', 'https://hr.example.com', '管理系统', 'ACTIVE', 4),
('财务系统', '财务管理系统', '💰', 'https://finance.example.com', '业务系统', 'ACTIVE', 5),
('项目管理', '项目协作平台', '📋', 'https://pm.example.com', '协作工具', 'ACTIVE', 6);
```

### 用户角色数据

```sql
-- 插入用户角色
INSERT INTO `user_roles` (`user_id`, `role_code`) VALUES
(1, 'ADMIN'),
(2, 'DEVELOPER');
```

## 数据库脚本

### 完整初始化脚本

创建文件 `src/main/resources/db/migration/V1__init_schema.sql`:

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS `xy_portal` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE `xy_portal`;

-- 创建用户表
CREATE TABLE `users` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `username` VARCHAR(255) NOT NULL,
  `password` VARCHAR(255) NOT NULL,
  `nickname` VARCHAR(255) DEFAULT NULL,
  `email` VARCHAR(255) DEFAULT NULL,
  `avatar` VARCHAR(512) DEFAULT NULL,
  `role` VARCHAR(50) NOT NULL DEFAULT 'USER',
  `status` VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_email` (`email`),
  KEY `idx_role` (`role`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

-- 创建门户入口表
CREATE TABLE `portal_entries` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(255) NOT NULL,
  `description` VARCHAR(512) DEFAULT NULL,
  `icon` VARCHAR(255) DEFAULT NULL,
  `url` VARCHAR(512) NOT NULL,
  `category` VARCHAR(50) DEFAULT NULL,
  `status` VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
  `sort_order` INT DEFAULT 0,
  `created_by` BIGINT DEFAULT NULL,
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_category` (`category`),
  KEY `idx_status` (`status`),
  KEY `idx_sort_order` (`sort_order`),
  KEY `idx_created_by` (`created_by`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='门户入口表';

-- 创建用户角色表
CREATE TABLE `user_roles` (
  `id` BIGINT NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL,
  `role_code` VARCHAR(50) NOT NULL,
  `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_role` (`user_id`, `role_code`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_role_code` (`role_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户角色表';
```

### 初始化数据脚本

创建文件 `src/main/resources/db/data.sql`:

```sql
USE `xy_portal`;

-- 插入管理员用户（密码：admin123，BCrypt加密）
INSERT INTO `users` (`username`, `password`, `nickname`, `email`, `role`, `status`) VALUES
('admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iwy8pK0O', '管理员', 'admin@example.com', 'ADMIN', 'ACTIVE'),
('user1', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iwy8pK0O', '用户1', 'user1@example.com', 'USER', 'ACTIVE');

-- 插入示例入口
INSERT INTO `portal_entries` (`name`, `description`, `icon`, `url`, `category`, `status`, `sort_order`) VALUES
('ERP系统', '企业资源规划系统', '📊', 'https://erp.example.com', '业务系统', 'ACTIVE', 1),
('CRM系统', '客户关系管理系统', '👥', 'https://crm.example.com', '业务系统', 'ACTIVE', 2),
('OA系统', '办公自动化系统', '📝', 'https://oa.example.com', '协作工具', 'ACTIVE', 3),
('HR系统', '人力资源管理系统', '👔', 'https://hr.example.com', '管理系统', 'ACTIVE', 4),
('财务系统', '财务管理系统', '💰', 'https://finance.example.com', '业务系统', 'ACTIVE', 5),
('项目管理', '项目协作平台', '📋', 'https://pm.example.com', '协作工具', 'ACTIVE', 6);

-- 插入用户角色
INSERT INTO `user_roles` (`user_id`, `role_code`) VALUES
(1, 'ADMIN'),
(2, 'DEVELOPER');
```

## 索引优化建议

1. **users 表**:
   - `username` 唯一索引，用于登录查询
   - `email` 索引，用于邮箱查询
   - `role` 索引，用于角色筛选

2. **portal_entries 表**:
   - `category` 索引，用于分类筛选
   - `status` 索引，用于状态筛选
   - `sort_order` 索引，用于排序查询
   - `created_by` 索引，用于创建人查询

3. **user_roles 表**:
   - `user_id` 和 `role_code` 联合唯一索引，防止重复
   - 分别建立单列索引，用于关联查询

## 数据安全

1. **密码加密**: 使用 BCrypt 算法加密存储
2. **敏感数据**: 邮箱、头像等敏感信息可考虑加密存储
3. **软删除**: 考虑添加 `deleted_at` 字段实现软删除
4. **审计日志**: 重要操作记录操作人和操作时间

## 扩展建议

1. **用户表扩展**:
   - 添加 `phone` 字段（手机号）
   - 添加 `department` 字段（部门）
   - 添加 `last_login_at` 字段（最后登录时间）

2. **入口表扩展**:
   - 添加 `visit_count` 字段（访问次数）
   - 添加 `last_visit_at` 字段（最后访问时间）
   - 添加 `tags` 字段（标签，JSON格式）

3. **权限表扩展**:
   - 创建 `permissions` 表（权限表）
   - 创建 `role_permissions` 表（角色权限关联表）

